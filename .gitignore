# merged image data
docker-all/data/

# pulled repo
dpfproto/data
dpfproto/logs/

.venv/

# ---- large generated data (regenerate via the gen pipelines) ----
results/parquet/
duckdb/results

# ---- benchmark intermediates: results/all_results.csv is the ONLY kept results
# file; every per-run / per-SF / detail CSV is a throwaway (runners merge into
# all_results.csv via merge_results.py and delete their temp).
results/query_times_*.csv
results/*_logs/
**/_raw/
**/_tmp/
**/_tmp_pq/
**/_work/
_spark_scratch/
_polars_scratch/
_polars_scratch_gpu/
docker/data/

# ---- python venvs & caches ----
polars/.venv/
polars/.venv-gpu/
**/__pycache__/
*.pyc

# ---- large binaries / envs (re-downloadable, see READMEs) ----
rapids/jars/
rapids/env/

# ---- upstream clones (re-cloneable; URLs documented in READMEs) ----
/lancelot/
/spark-rapids-benchmarks/
/tpch-kit/
# the sirius clone + its .pixi build env (setup_sirius.sh recreates it)
/sirius/sirius/

# ---- sirius spill scratch ----
_sirius_spill/

# ---- run logs / captures ----
*.log
*.out
